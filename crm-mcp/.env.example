# --- Lesen (Pflicht): deine bestehende Read-only-Rolle ---
DATABASE_URL=postgresql://dino_crm_discovery_ro_v1:PASSWORT@db.xxx.supabase.co:5432/postgres

# --- Schreiben (optional, nur fuer Historie/Profile/Freigaben/Export-Log) ---
# Ohne diese URL laufen Lese-Tools normal, Schreib-Tools melden "nicht konfiguriert"
DATABASE_WRITE_URL=postgresql://crm_mcp_writer:PASSWORT@db.xxx.supabase.co:5432/postgres

# SSL bei Supabase/Neon = true
DB_SSL=true

# Export-Dateien hier ablegen (leer lassen = Export als Base64 im Chat)
EXPORT_DIR=
